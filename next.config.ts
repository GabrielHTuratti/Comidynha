import type { NextConfig } from "next";

const nextConfig = {
  basePath: '', // Defina o basePath como vazio se estiver na raiz
  assetPrefix: '', // Defina o assetPrefix como vazio se estiver na raiz
};




export default nextConfig;
